# Terraform part

- version.tf -> to define the terarform and provider version
- variabkles.tf -> put all variables
- network.tf -> vpc, subnet, RT and all that stuff
- ecs.tf -> ecs resources
- rds.tf -> rds db part
- sg.tf -> all security groups
- alb.tf -> load balancer part